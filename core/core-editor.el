;;; core-editor.el --- 编辑行为设置 -*- lexical-binding: t -*-
;;; Commentary:

;; 基础编辑行为配置：括号匹配、选择替换、代码折叠、自动刷新等。
;; 不依赖任何第三方包，仅使用 Emacs 内置功能。

;;; Code:

;; ============================================================================
;; 括号
;; ============================================================================

;; 自动补全括号
(electric-pair-mode t)

;; 编程模式下，光标在括号上时高亮另一个括号
(add-hook 'prog-mode-hook #'show-paren-mode)

;; ============================================================================
;; 选择与编辑
;; ============================================================================

;; 选中文本后输入文本会替换文本（更符合其他编辑器的习惯）
(delete-selection-mode t)

;; ============================================================================
;; 代码折叠
;; ============================================================================

;; 编程模式下启用代码折叠
(add-hook 'prog-mode-hook #'hs-minor-mode)
;; C-c C-f	web-mode-fold-or-unfold	折叠/展开当前 HTML 标签或代码块

;; 更方便的折叠快捷键
(with-eval-after-load 'hideshow
  (define-key hs-minor-mode-map (kbd "C-c h") #'hs-toggle-hiding)
  (define-key hs-minor-mode-map (kbd "C-c H") #'hs-hide-all)
  (define-key hs-minor-mode-map (kbd "C-c S") #'hs-show-all))

;; ============================================================================
;; 文件自动刷新
;; ============================================================================

;; 当外部程序修改了文件时，自动刷新 Buffer
(global-auto-revert-mode t)

;; ============================================================================
;; 杂项
;; ============================================================================

;; 增大 Lisp 求值深度限制
(setq max-lisp-eval-depth 10000)

(provide 'core-editor)
;;; core-editor.el ends here
