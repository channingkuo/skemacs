## [今日] Checkpoint: 2026-04-15

### 完成了
- [x] /init-project 初始化项目结构（.claude/docs/、全局注册表、CLAUDE.md）
- [x] 诊断修复 markdown-mode 预览报错（"Markdown command markdown is not found"）
- [x] 安装 pandoc，配置 markdown-command + live preview eww + delete-on-destroy
- [x] 更新 CLAUDE.md：新增 superpowers 文档路径规范（.claude/docs/superpowers/）
- [x] 写入设计文档 .claude/docs/superpowers/specs/2026-04-15-markdown-preview-design.md
- [x] 写入实现计划 .claude/docs/superpowers/plans/2026-04-15-markdown-preview.md
- [x] 所有变更已 commit（HEAD: 51fd1d8）

### 进行中
- [ ] 用户验证 markdown-live-preview-mode 的 delete-on-destroy 行为
  下一步：用 `C-c C-c l` toggle 关闭 mode（不是关 eww 窗口），确认临时 HTML 是否被删除

### 决策记录
- **Markdown 预览后端**：选择 pandoc + eww（方案 C），原因：无需网络、无额外 Python 环境、渲染质量优于纯 eww
  - 放弃 grip-mode（方案 B），因依赖 GitHub API 网络
- **superpowers 文档路径**：统一到 .claude/docs/superpowers/，不放项目根 docs/

### 遗留问题
- delete-on-destroy 效果待用户最终确认：需用 C-c C-c l toggle 关闭 mode，而非直接关 eww 窗口

### Git 状态
51fd1d8 feat: 关闭 markdown live preview 时自动删除临时 HTML 文件
ccae497 feat: 配置 markdown-mode 使用 pandoc + eww 实现 live preview
无未提交变更
